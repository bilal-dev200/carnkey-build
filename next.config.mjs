/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["192.168.111.155", "carnkey.plantinart.com", "cashoffer.accu-trade.com"], // 👈 yahan apna IP ya domain add karo
  },
};

export default nextConfig;
